# ლექცია 1 — კოდის დაყოფა (Code Splitting)

## 1.1 რა არის Code Splitting?

Code Splitting გულისხმობს JavaScript bundle-ის დაყოფას მცირე ნაწილებად (chunks), რათა მომხმარებელმა ჩატვირთოს მხოლოდ ის კოდი, რომელიც მას ამ მომენტში სჭირდება.

### რატომ არის მნიშვნელოვანი?
- ტიპიური React აპლიკაციის bundle შეიძლება იყოს 500KB-1MB+
- მომხმარებელს არ სჭირდება მთელი აპლიკაციის კოდი პირველივე გვერდზე
- ნელი ინტერნეტით მომხმარებლისთვის განსაკუთრებით კრიტიკულია

## 1.2 Dynamic Import (ES Modules)

```javascript
// ❌ ჩვეულებრივი import — ყველაფერი ერთ bundle-ში
import { heavyFunction } from './heavyModule';

// ✅ Dynamic import — ცალკე chunk, ჩაიტვირთება საჭიროებისამებრ
const module = await import('./heavyModule');
module.heavyFunction();

// პრაქტიკული მაგალითი — CSV ექსპორტი მხოლოდ ღილაკზე click-ით
document.getElementById('exportBtn').addEventListener('click', async () => {
  const { exportToCSV } = await import('./csvExporter');
  exportToCSV(data);
});
```

## 1.3 React.lazy() და Suspense

```jsx
import React, { Suspense, lazy } from 'react';

// ❌ ჩვეულებრივი import — ყველაფერი ერთ bundle-ში
// import Dashboard from './pages/Dashboard';
// import Settings from './pages/Settings';

// ✅ Lazy import — ცალკე chunk თითოეული გვერდისთვის
const Dashboard = lazy(() => import('./pages/Dashboard'));
const Settings = lazy(() => import('./pages/Settings'));

function App() {
  return (
    <Suspense fallback={<div>იტვირთება...</div>}>
      <Routes>
        <Route path="/dashboard" element={<Dashboard />} />
        <Route path="/settings" element={<Settings />} />
      </Routes>
    </Suspense>
  );
}
```

## 1.4 Route-based vs Component-based Splitting

### Route-based (ყველაზე ბუნებრივი):
```jsx
const Home = lazy(() => import('./pages/Home'));
const About = lazy(() => import('./pages/About'));
const Products = lazy(() => import('./pages/Products'));
const Cart = lazy(() => import('./pages/Cart'));
```

### Component-based (მძიმე კომპონენტებისთვის):
```jsx
const HeavyChart = lazy(() => import('./components/HeavyChart'));

function ArticlePage() {
  const [showChart, setShowChart] = useState(false);
  
  return (
    <div>
      <article>...</article>
      <button onClick={() => setShowChart(true)}>გრაფიკის ჩვენება</button>
      {showChart && (
        <Suspense fallback={<div>გრაფიკი იტვირთება...</div>}>
          <HeavyChart />
        </Suspense>
      )}
    </div>
  );
}
```

## 1.5 Tree Shaking

```javascript
// math.js
export function add(a, b) { return a + b; }
export function subtract(a, b) { return a - b; }
export function multiply(a, b) { return a * b; }

// app.js — მხოლოდ add გამოიყენება
import { add } from './math';
// build output-ში subtract და multiply არ იქნება

// ❌ Tree shaking ვერ მუშაობს CommonJS-ით
const { add } = require('./math'); // მთლიანი ფაილი ჩაიტვირთება

// ✅ ბიბლიოთეკების სწორი import
import orderBy from 'lodash-es/orderBy';    // მხოლოდ orderBy
// ❌ import { orderBy } from 'lodash';       // მთლიანი lodash (70KB+)
```

---

## სავარჯიშო

1. შექმენით React პროექტი: `npm create vite@latest split-demo -- --template react`
2. შექმენით 4 გვერდი (Home, About, Dashboard, Settings)
3. ჯერ **ჩვეულებრივი import** — `npm run build` → ჩაინიშნეთ bundle ზომა
4. გადაიყვანეთ **React.lazy**-ზე — `npm run build` → შეადარეთ
5. DevTools Network tab-ში დააკვირდით chunks-ის ჩატვირთვას ნავიგაციისას
